import {SafeAreaView, StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const SeeAllVideo = () => {
  return (
    <SafeAreaView>
      <Text>SeeAllVideo</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});
