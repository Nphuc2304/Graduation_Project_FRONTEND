import {SafeAreaView, StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SeeAllVideo = () => {
  return (
    <SafeAreaView>
      <Text>SeeAllVideo</Text>
    </SafeAreaView>
  );
};

export default SeeAllVideo;

const styles = StyleSheet.create({});
