import {ScrollView, Text, View} from 'react-native';
import colors from '../Color';

const Home = ({navigation}: any) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Home;
